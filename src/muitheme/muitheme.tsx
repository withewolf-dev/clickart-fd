import { createTheme, ThemeOptions } from "@mui/material/styles";

const Miuitheme: ThemeOptions = createTheme({
  palette: {
    primary: {
      main: "#000000",
      light: "#00000",
      dark: "#000000",
    },
  },
});
export default Miuitheme;
